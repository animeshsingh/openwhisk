{# this template is used to generate a config.json for booting a consul server #}
{
    "server": true,
    "data_dir": "/consul/data",
    "ui": true,
    "log_level": "WARN",
    "node_name": "{{ kubeips.consulservers.pod_ip }}",
    "client_addr": "0.0.0.0",
    "advertise_addr": "{{ kubeips.consulservers.pod_ip }}",
    "ports": {
        "dns": 8600
    },
    "bootstrap": true,
    "disable_update_check": true
}
